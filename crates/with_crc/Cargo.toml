[package]
name = "with_crc"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
serde = { workspace = true }
with_crc_derive = { path = "../with_crc_derive", optional = true }

[features]
derive = ["with_crc_derive"]